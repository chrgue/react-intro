import { Toolbar } from './Toolbar';

export function Header() {
    return (
        <header style={{ marginBottom: 12 }}>
            <h1>My App</h1>
            <Toolbar />
        </header>
    );
}